# 验收清单

- [ ] `doctor` 同时通过飞书机器人和模型连通检查。
- [ ] 服务端长连接成功，systemd 状态为 `active (running)`。
- [ ] 后台已订阅 `im.message.receive_v1`，且不是只有长连接方式而没有事件。
- [ ] 已开通获取群组中所有消息的权限，而不是只有接收 @ 机器人消息权限。
- [ ] 只收集阶段日志显示“发送=关闭（只收集）”。
- [ ] 普通文字和富文本能进入本地 SQLite。
- [ ] 相同 `message_id` 不会重复保存。
- [ ] 昵称能和群成员对应。
- [ ] `#总结` 能回复当天总结。
- [ ] 总结只引用群内原文，不编造负责人、时间或结论。
- [ ] 不同群的消息不会混在一起。
- [ ] 同一群同一天的定时总结默认只发送一次。
- [ ] 上海时间 22:00 的真实自动发送至少观察一次。
